import { BytesLike } from '@ethersproject/bytes';
import { JsonAbiFragmentType, Interface, JsonAbi } from '@fuel-ts/abi-coder';
import { AbstractPredicate, AbstractAddress } from '@fuel-ts/interfaces';

declare class Predicate extends AbstractPredicate {
    bytes: Uint8Array;
    address: AbstractAddress;
    types?: ReadonlyArray<JsonAbiFragmentType>;
    interface?: Interface;
    constructor(bytes: BytesLike, types?: JsonAbi);
}

export { Predicate };
